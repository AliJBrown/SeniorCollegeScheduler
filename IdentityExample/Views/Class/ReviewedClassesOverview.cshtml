@model ICollection<ClassSummaryViewModel>
@{
    ViewData["Title"] = "Reviewed Classes";
}

<h2 style="text-align:center;"> @ViewData["Title"]</h2>

<div class="table-responsive">
    <table class="table table-striped recipe">
        <thead>
            <tr>
                <th>Proposed ID</th>
                <th>Proposed Date</th>
                <th>Proposed Title</th>
                <th>Instructor Name</th>
                <th>City</th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var proposedClass in Model)
            {
                <tr>
                    <td>@proposedClass.ProposedID</td>
                    <td>@proposedClass.ProposedDate</td>
                    <td>@proposedClass.ProposedTitle</td>
                    <td>N/A</td>
                    <td>N/A</td>
                    <td><a asp-action="ProposedClassDetails" asp-route-id="@proposedClass.ProposedID" class="btn">View</a></td>
                </tr>
            }
        </tbody>
    </table>
</div>


